name: Sonar
on:
  push:
    branches:
      - master
      - develop
      - cleanup_code
jobs:
  sonarcloud:
    name: SonarCloud
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - name: Update ubuntu
        run: sudo apt update -y
      - name: Install missing library
        run: sudo apt-get install libnotify4 libsdl1.2debian libsdl2.2 wxpython-tools -y
      - name: Install xorg
        run: sudo apt-get install -y xorg openbox
      - name: Install Xvfb
        run: sudo apt-get install xvfb -y
      - name: Setup Python
        uses: actions/setup-python@v3
        with:
          python-version: 3.13
      - name: Install RIDE dependencies
        run: |
          git fetch --prune --depth=1 --recurse-submodules
          git submodule update --init --recursive
          python -m pip install --only-binary :all: -U pip
          python -m pip install --only-binary :all: https://extras.wxpython.org/wxPython4/extras/linux/gtk3/ubuntu-24.04/wxpython-4.2.5-cp313-cp313-linux_x86_64.whl
          pip install --require-hashes --only-binary :all: -r requirements-dev3.13.txt
      - name: Run Xvfb
        run: Xvfb :1 -noreset &
      - name: Test Install RIDE
        run: |
          git submodule update --init --recursive
          pip install --require-hashes --only-binary :all: -r requirements-dev3.13.txt
          pip install --only-binary :all: .
      - name: Run RIDE unit-tests
        run: |
          export DISPLAY=:1
          git submodule update --init --recursive
          pip install --require-hashes --only-binary :all: -r requirements-dev3.13.txt
          pip install --only-binary :all: .
          invoke test-ci
      - name: Analyze with SonarCloud
        uses: sonarsource/sonarqube-scan-action@master
        with:
          projectBaseDir: ${{ github.workspace }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}  # Needed to get PR information, if any
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          DISPLAY: :1

